services:
  audio_extractor_api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: audio_extractor_api
    ports:
      - "8012:8012"
    environment:
      API_KEY: ""
    restart: unless-stopped
